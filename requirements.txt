# Dependencias de twistlock-export
#
# openpyxl — generación del fichero .xlsx de salida.
# El resto del proyecto usa únicamente la biblioteca estándar de Python
# (csv, argparse, re, collections, datetime, pathlib), por lo que esta
# es la única dependencia externa necesaria.
#
# Instalación en entorno virtual (ver README.md para instrucciones completas):
#   pip install -r requirements.txt

openpyxl>=3.1.0
